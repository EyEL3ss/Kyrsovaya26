<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UltimateWalletFinal.Views.CardDetail"
             Title="Обзор карты">
    <ScrollView>
    <VerticalStackLayout Spacing="20" Padding="20">

        <Label x:Name="organisationNameLabel"
           FontSize="24"
           FontAttributes="Bold"
           HorizontalOptions="Center"/>

        <!-- Button с иконкой и текстом -->
            <HorizontalStackLayout Spacing="10" 
                           HorizontalOptions="Center"
                           VerticalOptions="Center">
            </HorizontalStackLayout>

        <Border BackgroundColor="#F5F5F5" 
           Padding="15">
            <VerticalStackLayout Spacing="10">
                    <Label Text="{Binding Id, StringFormat='Номер в списке: {0}'}"></Label>
                    <Label Text="{Binding CardName, StringFormat='Наименование карты: {0}'}"
                   FontSize="14"
                   TextColor="Gray"
                   FontAttributes="Bold"/>
                    <Label Text="{Binding CardDescription, StringFormat='Описание карты: {0}'}"
                   FontSize="14"
                   TextColor="Gray"
                   FontAttributes="Bold"/>
                    <Label
                               Text="{Binding CardNumber, StringFormat='Номер карты: {0}'}"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="#303F9F"
                               HorizontalOptions="Center"
                               Padding="15,10" />
                            <Grid RowDefinitions="Auto, *, Auto" 
ColumnDefinitions="*, *">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="CVV/CVC код"
                               FontSize="9"
                               TextColor="#757575" />
                            <Label x:Name="cvvLabel"
                               Text="•••"
                               FontSize="9"
                               FontAttributes="Bold"
                               TextColor="#212121" />
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                            Text="Показать"
                            Clicked="OnShowCvvClicked"
                            BackgroundColor="Transparent"
                            TextColor="#5C6BC0"
                            FontSize="9"
                            VerticalOptions="Center"
                            HeightRequest="30" />
                    </Grid>
                </VerticalStackLayout>
        </Border>

        
        
        
        
        
        
        <Border BackgroundColor="#F5F5F5" 
           Padding="15">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{Binding Shop.ShopName, StringFormat='Производитель: {0}'}"
                   FontSize="12"
                   TextColor="Gray"
                   FontAttributes="Bold"/>
                    <Label Text="{Binding Category.CategoryName, StringFormat='Категория: {0}'}"
                   FontSize="12"
                   TextColor="Gray"
                   FontAttributes="Bold"/>
                    <Button Text="ℹ️ Подробнее о магазине"
                            Clicked="OnShopInfoClicked"
                            BackgroundColor="Transparent"
                            TextColor="#2196F3"
                            FontSize="10"
                            HorizontalOptions="Start"
                            Margin="0,5,0,0"
                            Padding="0" />
                </VerticalStackLayout>
        </Border>

        <Border BackgroundColor="#F5F5F5" 
           Padding="15">
            <VerticalStackLayout Spacing="10">
                    <Frame x:Name="expiryIndicator"
                           Padding="5,2"
                           CornerRadius="5"
                           BackgroundColor="Transparent"
                           HorizontalOptions="Start"
                           Margin="0,5,0,0">
                        <Label x:Name="expiryStatusLabel"
                               FontSize="10"
                               FontAttributes="Bold" />
                    </Frame>
                    <Label x:Name="descriptionLabel"
                   FontSize="16"/>
                    <Label Text="📝 Зарегистрирована"
                           FontSize="12"
                           TextColor="#757575" />
                    <Label Text="{Binding CardCreateDate}"
                           FontSize="12"
                           TextColor="#212121"
                           Margin="0,2,0,0" />
                    <Label Text="📅 Срок действия"
                           FontSize="12"
                           TextColor="#757575" />
                    <Label Text="{Binding CardDate}"
                           FontSize="14"
                           FontAttributes="Bold"
                           TextColor="#212121"
                           Margin="0,2,0,0" />
                    <!-- Последнее использование -->
                    <VerticalStackLayout Grid.Column="1">
                        <Label Text="⏰ Последнее использование"
                           FontSize="12"
                           TextColor="#757575" />
                        <Label Text="{Binding LastUse}"
                           FontSize="12"
                           TextColor="#212121"
                           Margin="0,2,0,0" />
                    </VerticalStackLayout>
                    <Grid RowDefinitions="Auto, *, Auto" 
      ColumnDefinitions="*, *">

                        <!-- Избранное -->
                        <Button x:Name="favoriteButton"
                    Text="В избранное"
                    Clicked="OnToggleFavoriteClicked"
                    BackgroundColor="#FF4081"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="45"
                    FontSize="14" />

                        <!-- Удалить -->
                        <Button Grid.Column="1"
                    Text="🗑️ Удалить"
                    Clicked="OnDeleteClicked"
                    BackgroundColor="#F44336"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="45"
                    FontSize="14" />

                    </Grid>

                    <!-- Кнопка копирования данных -->
                    <Button Text="📋 Скопировать данные карты"
                Clicked="OnCopyDataClicked"
                BackgroundColor="#2196F3"
                TextColor="White"
                CornerRadius="10"
                HeightRequest="40"
                FontSize="12"
                Margin="0,5,0,0" />
                </VerticalStackLayout>
        </Border>
    </VerticalStackLayout>
</ScrollView>
</ContentPage>